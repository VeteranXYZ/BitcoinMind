---
import Base from '@/layouts/Base.astro';
import { QUESTIONS } from '@/data/questions';
import { SITE } from '@/lib/seo';
---
<Base
  title="Questions"
  description="Selected reader questions about Bitcoin, answered when there is something worth saying. Objections, custody, philosophy, and practice."
  path="/questions"
>
  <div class="page">
    <div class="c">
      <div class="ph">
        <div class="ph-eye">Questions</div>
        <h1 class="ph-title">Open <em>Questions</em></h1>
        <p class="ph-desc">Selected reader questions, answered when there is something worth saying.</p>
      </div>

      <div class="qa-list">
        {QUESTIONS.map((q, i) => (
          <div class="qa-item" id={q.id}>
            <div class="qa-q-row">
              <span class="qa-num">Q{String(i + 1).padStart(2, '0')}</span>
              <h2 class="qa-question">{q.question}</h2>
            </div>
            <div class="qa-answer-wrap">
              <div class="qa-meta">
                <span class="qa-date">{q.date}</span>
                <span class="qa-by">Hiei</span>
              </div>
              <div class="qa-answer">
                <p>{q.answer}</p>
              </div>
              {q.links && (
                <div class="qa-links">
                  {q.links.map((l) => (
                    <a class="qa-link" href={l.href}>{l.label} →</a>
                  ))}
                </div>
              )}
            </div>
          </div>
        ))}
      </div>

      <div class="qa-submit">
        <div class="qa-submit-hd">
          <div class="a-eye">Submit</div>
          <h2 class="qa-submit-title">Have a question?</h2>
        </div>
        <p class="qa-submit-body">If you have a question about Bitcoin that you think deserves a thoughtful answer — not a price prediction, not "when moon" — send it. I read everything. I answer what I can.</p>
        <a class="qa-submit-cta" href={`mailto:${SITE.contactEmail}?subject=Question for BitcoinMind`} target="_blank" rel="noopener noreferrer">
          <span class="qa-submit-arrow">→</span>
          <span>{SITE.contactEmail}</span>
        </a>
      </div>
    </div>
  </div>
</Base>

<style>
  .qa-list {
    max-width: 768px;
  }

  .qa-item {
    margin-bottom: var(--sp-11);
    padding-bottom: var(--sp-11);
    border-bottom: 1px solid var(--bdr);
  }
  .qa-item:last-child {
    margin-bottom: 0;
    padding-bottom: 0;
    border-bottom: none;
  }

  .qa-q-row {
    display: flex;
    align-items: flex-start;
    gap: var(--sp-4);
    margin-bottom: var(--sp-5);
  }

  .qa-num {
    font-family: var(--font-mono);
    font-size: var(--fz-3xs);
    color: var(--gold);
    letter-spacing: var(--ls-5);
    border: 1px solid var(--border-accent);
    padding: 3px var(--sp-2);
    border-radius: var(--radius-sm);
    flex-shrink: 0;
    /* Optical alignment with first line of question text */
    margin-top: var(--sp-1);
  }

  .qa-question {
    font-family: var(--font-body);
    font-size: var(--fz-xl);
    font-weight: var(--fw-bold);
    letter-spacing: var(--ls-snug);
    line-height: var(--lh-snug);
    color: var(--tx);
  }

  .qa-answer-wrap {
    /* Align with question text: qa-num width + gap */
    padding-left: var(--sp-8);
  }

  .qa-meta {
    display: flex;
    align-items: center;
    gap: var(--sp-3);
    margin-bottom: var(--sp-3);
  }

  .qa-date {
    font-family: var(--font-mono);
    font-size: var(--fz-3xs);
    color: var(--tx-3);
    letter-spacing: var(--ls-7);
    text-transform: uppercase;
  }

  .qa-by {
    font-family: var(--font-body);
    font-size: var(--fz-3xs);
    color: var(--gold);
    letter-spacing: var(--ls-7);
    text-transform: uppercase;
  }

  .qa-answer {
    font-family: var(--font-body);
    font-size: var(--fz-md);
    line-height: var(--lh-loose);
    color: var(--tx-2);
  }

  .qa-links {
    display: flex;
    flex-wrap: wrap;
    gap: var(--sp-4);
    margin-top: var(--sp-3);
  }

  .qa-link {
    font-family: var(--font-body);
    font-size: var(--fz-xs);
    color: var(--gold);
    letter-spacing: var(--ls-3);
    transition: color var(--ease-fast);
  }
  .qa-link:hover {
    color: var(--gold-hi);
  }

  /* Submit section */
  .qa-submit {
    max-width: 768px;
    margin-top: var(--sp-9);
    padding: var(--sp-7) var(--sp-7);
    background: var(--surf);
    border: 1px solid var(--bdr);
    border-radius: var(--radius);
  }

  .qa-submit-hd {
    margin-bottom: var(--sp-3);
  }

  .qa-submit-title {
    font-family: var(--font-body);
    font-size: var(--fz-2xl);
    font-weight: var(--fw-bold);
    letter-spacing: var(--ls-snug);
  }

  .qa-submit-body {
    font-family: var(--font-body);
    font-size: var(--fz-md);
    line-height: var(--lh-relaxed);
    color: var(--tx-2);
    margin-bottom: var(--sp-5);
  }

  .qa-submit-cta {
    display: inline-flex;
    align-items: center;
    gap: var(--sp-3);
    font-family: var(--font-body);
    font-size: var(--fz-sm);
    color: var(--tx);
    padding: var(--sp-3) var(--sp-4);
    border: 1px solid var(--bdr);
    border-radius: var(--radius);
    transition: border-color var(--ease-fast), background var(--ease-fast);
  }
  .qa-submit-cta:hover {
    border-color: var(--border-accent);
    background: var(--surf-2);
  }

  .qa-submit-arrow {
    color: var(--gold);
    font-size: var(--fz-xs);
  }

  @media (max-width: 640px) {
    .qa-answer-wrap {
      padding-left: 0;
      margin-top: var(--sp-2);
    }
    .qa-submit {
      padding: var(--sp-5) var(--sp-4);
    }
  }
</style>
